---

## 角色与任务

你是一名资深全栈工程师，请帮我在当前 `submarine/` 目录下开发一个**海缆故障传报系统**。本目录已经包含完整的需求文档、数据文件和一份早期原型代码，请严格按以下流程推进。

---

## 第一步：通读上下文（必须先完成，不要跳过）

请按以下顺序阅读当前目录下的所有文件：

### 1. 需求文档（3 份 Markdown）—— 最权威的需求来源

- `海缆故障传报系统-业务背景说明.md`
- `金桥机房电路表-数据源说明.md`
- `海缆故障传报系统-功能需求说明.md`

### 2. 数据文件（2 份 Excel）—— 用代码打开看真实结构，不要猜

- **`金桥机房电路表.xlsx`** —— 输入数据源。请读取表头和前若干行，搞清楚实际列名、数据格式，尤其：
  - "第一路由 / 第二路由 / 第三路由"字段的真实写法样例
  - "电路性质" "电路带宽" "Site A / Site B / 客户名称" 的实际取值格式
- **`国际海缆故障影响业务统计表 TPE S4（崇明-淡水）.xlsx`** —— 这是一份**真实的输出样例**（附件成品）。系统生成的影响统计表应尽量贴合它的结构、表头、格式。请读取并作为输出模板的参考。

### 3. 海缆路由图资源

- `海缆路由图/` 文件夹 —— 查看内部图片文件，判断能否直接作为态势图的底图或参考。

### 4. 历史原型代码

- `cable_fault.py`、`server.py` —— 这是我上一次尝试的遗留代码，**请先评估**：
  - 代码质量如何？架构是否合理？
  - 是否覆盖了本次需求的部分功能？
  - 建议**完全重写**、**部分复用**、还是**在此基础上扩展**？
  - **给出你的评估结论后再动工**，不要默认复用也不要默认删除。

---

## 第二步：汇报与确认（在写代码之前必须完成）

读完上述所有内容后，请**先不要写代码**，先给我一份简短的汇报，包含：

1. **需求理解** —— 用 5-10 句话复述，确认无偏差
2. **真实数据特征** —— 金桥机房电路表的真实列名与数据特点，尤其路由字段的实际写法样例（至少给出 3-5 条真实路由字段的取值）
3. **旧代码处置建议** —— 评估结论 + 重写/复用/扩展的建议 + 理由
4. **技术栈选型** —— 你推荐的技术栈和**选型理由**（见第三步的约束）
5. **项目结构** —— 你计划的目录结构
6. **默认决策清单** —— 对三份需求文档末尾"待确认事项"中关键问题的默认决策（例如海缆段落匹配策略选 A/B/C、是否持久化、"上海落地"匹配规则等），列出来让我确认
7. **开发步骤拆分** —— 预计分几步完成，每步产出什么

等我确认后再开始编码。

---

## 第三步：技术栈选型（开放式，但有约束）

**由你自行选型**，但必须满足以下约束：

### 硬性要求

- **运行形态**：本地 Web 服务，用户通过浏览器访问（启动后访问 `http://localhost:xxxx`）
- **启动成本低**：用户执行一两条命令即可启动，**不要求**有 Docker、Node 构建链等重依赖的前置条件——除非你有强理由说服我
- **跨平台**：至少能在 macOS 和 Windows 上跑起来
- **中文友好**：界面、Excel 生成都要正确处理中文，不能乱码

### 选型建议方向（供参考，不强制）

- 后端：Python（Flask / FastAPI）或 Node.js，看哪个更贴近旧代码或更适合本项目
- 前端：原生 HTML/JS + CDN 引入的轻量 UI 库（如 Tailwind、Alpine.js），或 Vue/React 的单文件组件形态，**避免复杂构建工具链**
- Excel 处理：`openpyxl` 或 `pandas`（Python 生态成熟度高）
- 态势图：推荐**内嵌 SVG 方案**，易于控制样式和交互

### 持久化建议

- 故障事件状态保存在本地文件（如 `data/fault_state.json`），便于重启后恢复
- 海缆基础数据（8 条段落）**硬编码在配置模块中**，不依赖 Excel
- 金桥机房电路表**每次故障时实时读取**，保证数据最新

---

## 第四步：功能实现要点

完全以三份需求 md 文档为准，这里仅强调几个重点和补充：

### 核心业务规则（务必对照需求文档实现）

1. **海缆段落硬编码清单**：共 8 条（崇明 6 条 + 南汇 2 条），具体见《业务背景说明》第 2 节
2. **通报模板与示例**：严格使用《业务背景说明》第 4、5 节中的模板格式
3. **电路筛选与主备双断判定**：严格遵循《功能需求说明》第 4 节的判定规则
4. **海缆名称匹配**：采用宽松关键字匹配；对 `APCN2崇明` / `TPE崇明` 这类无法区分段落的情况，**默认采用策略 A（保守，全部纳入疑似清单并在前端标注"疑似"）**，可在第二步中建议更合理的方案
5. **电路性质筛选**：只统计 **IP** 和 **IEPL** 两类；IP 统计"带宽损失"，IEPL 统计"客户专线明细"
6. **上海落地判定**：以 `Site A == 上海` 为准，但实际数据中的写法待验证（可能是"上海"、"Shanghai"、含区县如"上海-临港"等），请在第二步汇报真实写法并给出匹配策略

### 界面布局建议

单页应用，分为以下区域：

- **上区：实时故障态势图** —— 可交互的海缆段落可视化，每条段落用颜色标识状态（正常 / 中断）
- **中区：故障录入表单** —— 下拉选择故障海缆 + 日期时间选择器 + 提交按钮
- **下区：通报输出** —— 分 tab 或分块展示：
  - 首报邮件文本（带复制按钮）
  - 首报短信文本（带复制按钮）
  - 续报邮件文本（带复制按钮）
  - 续报微信文本（带复制按钮）
  - 业务影响统计表下载按钮
- **侧边：故障事件时间线** —— 列出所有已录入的故障，支持手动"解除"某条海缆故障

### 态势图实现建议

优先使用**内嵌 SVG 示意图**方案：把 8 条海缆段落手动绘制成简洁的示意图（崇明 / 南汇两个登陆站 + 放射状的海缆线条 + 方向标签），通过 JS 动态改变线条颜色。如果发现 `海缆路由图/` 中的底图适合直接使用，再切换方案。

---

## 第五步：交付标准

- 代码可直接运行，启动命令简单（最好一条命令搞定）
- 提供一份清晰的 `README.md`，包含：
  - 系统简介
  - 环境依赖
  - 安装步骤
  - 启动方式
  - 使用说明（如何录入故障、如何复制通报、如何下载附件）
  - 数据文件位置说明（金桥电路表放在哪里、如何更新）
- 代码有必要的注释，尤其是业务逻辑部分（路由匹配、主备双断判定）
- 生成的四段通报文本完全符合模板，占位符正确填充
- 生成的 `国际海缆故障影响业务统计表.xlsx` 结构与样例文件保持一致
- **提供一个快速验证用例**：录入"2025-12-21 04:24 TPE S4 段故障"，输出应能对齐《业务背景说明》第 5 节示例中的文本（电路明细部分以金桥电路表真实内容为准）

---

## 第六步：工作纪律（重要）

1. **先读文档和数据再动手**，不要跳过第一步
2. **先汇报再开发**，不要跳过第二步
3. 遇到需求不明确的地方，优先参考三份 md 文档末尾的"待确认事项"，给出默认方案后继续；不要卡住等我
4. 代码要能**独立运行和自测**，每完成一个模块就跑一下确认无错
5. 如果发现需求文档与真实数据（Excel 表实际内容）有冲突，**以真实数据为准**，并在汇报中指出

---

现在，请开始第一步。
