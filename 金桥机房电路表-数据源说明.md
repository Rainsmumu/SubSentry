# 金桥机房电路表 - 数据源说明

## 1. 概述

在海缆故障发生后，系统需生成**业务影响情况续报**，其核心数据来源为 **`金桥机房电路表.xlsx`**。

该表是一份电路资料表，统计了所有客户的业务电路信息，包括电路的两端、客户名称、路由走向、带宽、性质等。系统需从该表中筛选出**受本次海缆故障影响的电路**，并生成通报内容与附件清单。

---

## 2. 关键列说明

> 注：金桥机房电路表列数较多，以下仅列出**本系统需要关心**的列。其他列（如具体路由设备信息）可忽略。

### 2.1 电路两端信息

| 列名 | 说明 |
| --- | --- |
| **Site A** | 电路 A 端站点 |
| **Site B** | 电路 Z 端站点（即另一端） |

**注意事项**：
- 电路两端之间**可能存在经转**（中转节点）。
- **`Site A` 为上海的电路**，表示该电路在**上海落地**，在通报中需单独统计"上海落地"的数量与带宽。

### 2.2 客户与业务信息

| 列名 | 说明 | 在通报中的用途 |
| --- | --- | --- |
| **客户名称** | 电路所属客户 | ⭐ 通报业务影响时必填 |
| **电路带宽** | 电路速率（如 100G、1G 等） | ⭐ 用于统计 IP 带宽损失、客户专线带宽 |
| **电路性质** | 电路业务类型 | ⭐ 用于筛选需纳入通报的电路 |

**电路性质**可能的取值及处理方式：

| 电路性质 | 含义 | 是否纳入海缆故障通报 |
| --- | --- | --- |
| **IP** | IP 业务电路 | ✅ 需统计（体现在"IP 损失 XXG 带宽"） |
| **IEPL** | 客户专线业务 | ✅ 需统计（体现在"客户专线中断"明细） |
| **IPLC / DDN / MPLS-VPN / NNI** | 其他客户专线类型 | ✅ 按客户专线统计并保留原始性质 |
| 通道 | 通道类业务 | ❌ 暂不纳入 |
| VOICE | 语音业务 | ❌ 暂不纳入 |
| 其他 | — | ❌ 暂不纳入 |

### 2.3 路由与海缆信息

电路的海缆路由信息分布在以下列中：

| 列名 | 说明 |
| --- | --- |
| **第一路由** | 电路的主路由（对应的海缆段落） |
| **第二路由** | 电路的保护路由（SNCP - 业务级别保护） |
| **第三路由** | 保护路由上的再保护（端口 / 通道级别保护） |

**路由结构说明**：

受历史原因及电路保护机制影响，电路的路由数量不一，可分为三类：

- **单腿电路**：仅有第一路由
- **两腿电路**：有第一路由 + 第二路由（带业务级保护）
- **三腿电路**：有第一路由 + 第二路由 + 第三路由（带业务级保护 + 端口/通道级保护）

判断某条电路是否受某海缆段落中断影响，需**逐一检查其所有路由**是否涉及该海缆段落。

### 2.4 可忽略的列

从 **`Equ_0`** 列开始及其后所有列，均为具体的路由设备信息（节点、端口等），**本系统无需使用**，在通报中也不涉及。

---

## 3. 已知数据质量问题

### 3.1 海缆名称格式不统一 ⚠️

由于历史遗留原因，金桥机房电路表中的**海缆路由命名格式不统一**，同一海缆段落可能存在多种写法。

**示例**：同一条海缆（NCP S3 段，南汇出口），在不同电路记录中可能被写作：

- `NCP南汇`
- `NCP S3`
- `南汇NCP`
- `NCP南汇`（与上写法看似相同但可能有空格、大小写差异）

带保护电路（两腿、三腿）由于路由多，命名不一致问题会更加复杂。

### 3.2 部分海缆段落无法区分 ⚠️

对于 **APCN2** 和 **TPE** 这两条海缆，由于其在崇明登陆站有**多个段落出口**，仅标注如下内容**无法区分具体段落**：

- `APCN2崇明` → 可能是 **APCN2 S3 段** 或 **APCN2 S4A 段**
- `TPE崇明` → 可能是 **TPE S1S 段** 或 **TPE S4 段**

带保护电路中该问题出现频率较高。

### 3.3 当前阶段的处理原则

上述数据质量问题**后期会在源数据中逐步修正**，在当前阶段：

- **系统无需尝试自动识别或修正**这些不规范命名
- 建议先按**尽可能宽松**的字符串匹配规则处理（如关键字包含匹配）
- 在筛选结果中，应允许用户**人工复核和调整**最终纳入通报的电路清单

---
