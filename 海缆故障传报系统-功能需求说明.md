# 海缆故障传报系统 - 功能需求说明

## 1. 功能总览

本系统包含三大核心功能模块：

| 模块 | 功能概述 |
| --- | --- |
| 模块一 | **实时故障态势展示**：以可视化地图形式展示海缆与故障情况 |
| 模块二 | **通报信息生成**：根据故障信息自动生成通报文本与业务影响附件 |
| 模块三 | **动态影响分析**：跟踪多条海缆相继故障时的电路主备双断情况 |

---

## 2. 模块一：实时故障态势展示

### 2.1 功能描述

提供一个**可交互的类地图组件**，在主界面上直观展示上海国际局负责的海缆及其段落的**实时故障状态**。

### 2.2 展示内容

- **海缆路由**：标注崇明、南汇两个登陆站出口的 8 条海缆段落（参见《业务背景说明》第 2 节）
- **故障状态**：以不同的视觉样式（如颜色、图标）区分以下状态：
  - 🟢 **正常**
  - 🔴 **中断**
  - 🟡 **待确认 / 处理中**（可选）
- **段落信息**：悬停或点击海缆段落时，展示该段落的基本信息（海缆名、段落、路由、方向、当前状态、中断时间等）

### 2.3 资源说明

- 海缆的出口路由底图存放于 **`海缆路由图/`** 文件夹下，系统需读取该文件夹中的图像作为地图背景或参考图层。

### 2.4 交互要求

- 当模块二（通报生成）中某海缆被标记为中断后，该组件需**实时同步更新**对应海缆段落的状态为"中断"。
- 支持用户点击海缆段落查看详情。
- （可选）支持手动切换海缆状态，便于演练或修正误报。

---

## 3. 模块二：海缆通报信息实时生成

### 3.1 功能描述

当海缆发生故障时，用户输入故障信息，系统**立即生成**所需的全套通报内容，包括：

- **首报**：邮件文本、短信文本
- **续报**：邮件文本、微信文本、业务影响情况附件（Excel）

### 3.2 用户输入

用户录入以下故障信息：

| 输入项 | 说明 | 是否必填 |
| --- | --- | --- |
| 故障海缆 | 从系统预设的 8 条海缆段落中下拉选择 | ✅ |
| 中断时间 | 精确到分钟的日期时间 | ✅ |

> 登陆站、海缆方向、路由（如"崇明-淡水"）等信息由系统根据所选海缆**自动填充**，无需用户录入。

### 3.3 系统输出

#### 3.3.1 通报文本（可复制）

系统根据模板（参见《业务背景说明》第 4 节）自动填充变量，生成以下四段文本，每段均**提供一键复制按钮**：

- 首报 - 邮件文本
- 首报 - 短信文本
- 续报 - 邮件文本
- 续报 - 微信文本

#### 3.3.2 业务影响情况表（可下载）

系统基于**金桥机房电路表**，筛选出受本次海缆故障影响的电路，生成 `国际海缆故障影响业务统计表.xlsx`，供用户下载并作为续报邮件附件。

筛选逻辑（详见模块三）。

#### 3.3.3 态势图同步更新

故障信息提交后，模块一的地图组件需立即将对应海缆段落状态更新为"中断"。

---

## 4. 模块三：动态影响分析（主备双断检测）

### 4.1 问题背景

当一条海缆中断时：

- **部分电路**只有单路由（单腿），或其路由恰好在该海缆上 → **立即中断**
- **部分电路**有保护路由（两腿、三腿） → **主路由断但保护路由存活，业务不中断**

但如果**另一条海缆再次故障**，且某条电路的**主备路由分别位于这两条海缆上**，就会发生 **主备双断**，导致业务中断。

这种**影响范围随故障累积而动态变化**的情况，是本模块需要解决的核心问题。

### 4.2 核心概念

| 概念 | 说明 |
| --- | --- |
| **已中断海缆集合** | 当前系统记录的所有处于"中断"状态的海缆段落 |
| **电路路由集合** | 某条电路涉及的所有路由（第一路由 / 第二路由 / 第三路由） |
| **电路状态** | 正常 / 中断（当电路的**所有路由**都落在"已中断海缆集合"中时，判定为中断） |

### 4.3 判定规则

对于金桥机房电路表中的每一条电路：

```
若 电路的所有路由 ⊆ 已中断海缆集合 → 该电路中断
反之，只要还有任意一条路由所在海缆未中断 → 该电路仍存活
```

**示例说明**：

| 电路类型 | 路由情况 | 当前已中断海缆 | 判定结果 |
| --- | --- | --- | --- |
| 单腿 | 仅第一路由：TPE S4 | {TPE S4} | 🔴 中断 |
| 两腿 | 主：TPE S4 / 备：APG S3 | {TPE S4} | 🟢 存活（备路由存活） |
| 两腿 | 主：TPE S4 / 备：APG S3 | {TPE S4, APG S3} | 🔴 中断（主备双断） |
| 三腿 | 主：TPE S4 / 备1：APG S3 / 备2：NCP S1.1 | {TPE S4, APG S3} | 🟢 存活（备2存活） |

### 4.4 动态更新机制

**建议方案**：系统维护一个**全局故障上下文**，每次故障事件都会触发一次**全量重算**。

#### 4.4.1 数据结构

```
故障上下文 = {
  已中断海缆集合: [海缆段落1, 海缆段落2, ...],
  故障事件列表: [
    { 海缆: TPE S4, 中断时间: 12-21 04:24 },
    { 海缆: APG S3, 中断时间: 12-22 09:00 },
    ...
  ]
}
```

#### 4.4.2 触发时机

每当用户录入一条新的海缆故障：

1. 将新故障海缆加入"已中断海缆集合"
2. **重新扫描**金桥机房电路表中的所有电路
3. 对每条电路，检查其所有路由是否均落在"已中断海缆集合"中
4. 标记出**新增中断**的电路（即本次故障新触发主备双断的那些电路）
5. 更新业务影响情况表，重新生成续报文本
6. 更新态势图显示

#### 4.4.3 用户体验

- 界面上可**区分展示**：
  - 本次故障**直接影响**的电路
  - 本次故障**累积触发主备双断**的电路（即因先前已有海缆故障，叠加本次故障后新增中断的电路）
- 支持用户**手动解除**某条海缆的故障状态（恢复后需要再次全量重算）
- 系统保留**故障事件时间线**，便于回溯查看不同时间点的业务影响情况

### 4.5 海缆名称匹配策略

由于金桥机房电路表的路由字段命名不规范（参见《数据源说明》第 3 节），在执行匹配判定时：

- 采用**宽松的关键字包含匹配**（如路由字段中包含 `NCP` 和 `南汇`，即可判定为 NCP S3 段）
- 对于无法明确区分段落的情况（如 `APCN2崇明`、`TPE崇明`），可采取以下策略之一（建议确认）：
  - **策略 A（保守）**：按"可能涉及"处理，将这类电路**全部纳入**疑似影响清单，由用户人工确认
  - **策略 B（宽松）**：视作两个段落**都涉及**，任一段落中断即认为该路由受影响
  - **策略 C（严格）**：仅当两个段落都中断时才认为该路由失效

---
